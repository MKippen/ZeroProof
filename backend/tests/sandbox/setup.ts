// Sandbox tests model a true fresh-install. tests/sandbox/freshInstall.test.ts
// also resets the User table and skips initializeDefaultUser() to enforce that.
// Nothing required here yet, but kept as a hook for future shared sandbox setup.
export {};
