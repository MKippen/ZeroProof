{
  "name": "@uguard/updater",
  "version": "1.0.0",
  "description": "Privileged sidecar that orchestrates ZeroProof self-upgrades. Receives HMAC-signed apply requests from backend; shells to scripts/upgrade.sh.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest --no-coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
